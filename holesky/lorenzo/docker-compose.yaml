volumes:
    op_geth_data:
    op_node_data:
    jwt_shared:
services:
    # ========================================
    # OPTIMISM NODE SERVICES
    # ========================================
    op-init:
        image: alpine:latest
        command:
            - sh
            - -c
            - |
              set -e

              # Create config directory
              mkdir -p /config

              # Generate JWT secret if it doesn't exist
              if [ ! -f '/jwt/jwt' ]; then
                echo 'Generating JWT secret'
                mkdir -p /jwt
                # Generate a 32-byte random hex string for JWT secret
                apk add --no-cache openssl
                openssl rand -hex 32 > /jwt/jwt
                chmod 666 /jwt/jwt
                echo 'JWT secret generated successfully'
              else
                echo 'JWT secret already exists, skipping generation'
              fi
        volumes:
            - op_node_data:/op-node
            - jwt_shared:/jwt
    op-geth-init:
        image: golemnetwork/golembase-l3-op-geth:v1.101511.0-0.2-holesky-lorenzo-4ebd8c2d
        depends_on:
            op-init:
                condition: service_completed_successfully
        entrypoint: []
        command:
            - /bin/sh
            - -c
            - |
              set -e
              if [ ! -f '/geth/geth' ]; then
                echo 'Initializing geth data directory with genesis block...'

                # Use genesis file from op-init
                if ! geth init --state.scheme=hash --datadir='/geth' '/genesis.json'; then
                  echo 'ERROR: Failed to initialize geth with genesis block'
                  exit 1
                fi
                echo 'Geth initialized successfully with genesis block'
              else
                echo 'Geth data directory already initialized, skipping initialization'
              fi
        volumes:
            - op_geth_data:/geth
    op-geth:
        image: golemnetwork/golembase-l3-op-geth:v1.101511.0-0.2-holesky-lorenzo-4ebd8c2d
        restart: unless-stopped
        stop_grace_period: 5m
        depends_on:
            op-geth-init:
                condition: service_completed_successfully
        command:
            - --networkid=9
            - --datadir=/geth
            - --http
            - --http.corsdomain=*
            - --http.vhosts=*
            - --http.addr=0.0.0.0
            - --http.port=8545
            - --http.api=admin,debug,eth,txpool,net,engine,web3,golembase,txpool
            - --ws
            - --ws.addr=0.0.0.0
            - --ws.port=8546
            - --ws.origins=*
            - --ws.api=admin,debug,eth,txpool,net,engine,web3,golembase,txpool
            - --syncmode=snap
            - --authrpc.vhosts=*
            - --authrpc.addr=0.0.0.0
            - --authrpc.port=8551
            - --authrpc.jwtsecret=/jwt/jwt
            - --usb=false
            - --state.scheme=hash
            - --nat=none
            - --bootnodes=enode://ca4176d5841283acbd4597809f593aa435676e87099b839417fbbc7566796b439db251a2ff8047026f50e865bcee95d70354a126dbee78d124a5a93919e8f636@49.13.30.29:30000,enode://192370e9e1e0770e15d4c929402b3d94a673ec8bdc32862cc493aaa35ecf8a75b57208500550d209f1e9259cc732193a8b0c9c5743a43cbeabf5adb745a35198@91.99.157.203:30002,enode://46ca40f4c122af54d530037f826e6e8f6867c7ed208f2a87bd0121843ad39abda9b1dce1f4ecd24f283646646f98732f8c9ae4dcca412cee6963f3137fd027d1@91.99.94.10:30004,enode://5e0a24a8878b9132e17be225e1657ced8ffdeb1f3fe5eb329060a22a82d79a997347cb1521b23267a51a6fdb481639ff2e6edf036cc6801aed4d3645fb997cd2@91.99.94.10:30006,enode://3dc2ba9e43a5beb9ced3aa02989b04e1dafb99dbd786adad5431609cfeccea29f3526f2e51ef55a240513064a46750e7e0c5c2970af73685b815ad715ac1fdf0@91.99.184.248:30008
            - --txpool.disable.non.golembase.transactions=true
        ports:
            - 8545:8545
            - 8546:8546
        volumes:
            - op_geth_data:/geth
            - jwt_shared:/jwt
    op-node:
        image: golemnetwork/golembase-l3-op-node:v1.13.4-holesky-lorenzo-84eab7e4
        restart: unless-stopped
        stop_grace_period: 5m
        depends_on:
            op-init:
                condition: service_completed_successfully
            op-geth:
                condition: service_started
        command:
            - op-node
            - --l1=https://rpc.l2.holesky.golem-base.io
            - --l1.beacon.ignore=true
            - --l1.rpckind=standard
            - --l1.trustrpc
            - --l2=http://op-geth:8551
            - --l2.enginekind=geth
            - --l2.jwt-secret=/jwt/jwt
            - --rpc.addr=0.0.0.0
            - --rpc.port=9545
            - --p2p.nat=true
            - --p2p.ban.peers=false
            - --p2p.bootnodes=enr:-Jy4QFycqYrXkOq6LCmh0CZlzMIJ-q0cFBecl0NRv11cXYMLevjf1ESM-5_ZVw2vp2x4IlpKWfAUjudY8Y_lWdDSeWGGAZhrFBHNgmlkgnY0gmlwhDENHh2Hb3BzdGFja4IJAIlzZWNwMjU2azGhApD_wUrFBM2MY3-uOf8AaqhpVDonVrCejKGIKPyCDkx6g3RjcIJ1MYN1ZHCCdTE,enr:-Jy4QJROmfy5UquEuQf1M7EN3YE6gAzCyNEfNMAOY6ml12O6N5bCOawtWUEK4TtOEWW3BuLNLL2W2ovW3VS5aLnop7uGAZhrFBTdgmlkgnY0gmlwhFtjncuHb3BzdGFja4IJAIlzZWNwMjU2azGhAtRAQ_NJzVgGRaWfD4gONWrDYuD64N1zMSZvO5C_a_1Ng3RjcIJ1M4N1ZHCCdTM,enr:-Jy4QDDHXmtYztLlVVT7FgQtki2hR3d2OBYtHcEEfpj28w72BWkD4eMtDJ6lBlag3yh3vcWD_Guh_DqPRM46b9q48ZOGAZhrFBC7gmlkgnY0gmlwhFtjXgqHb3BzdGFja4IJAIlzZWNwMjU2azGhAyapElFu6I25IaNsJ_bqnDD-3lxEujgNNKr-TqBwqzbyg3RjcIJ1NYN1ZHCCdTU,enr:-Jy4QM7KajS4NtZujmDwSnaEB2kvITQ3F8NiINizl53oKqKPe9_-VFrHqtFESxwEl8YZQxvoRrjQ2-U-UzCGdu6JQb2GAZhrFBRZgmlkgnY0gmlwhFtjXgqHb3BzdGFja4IJAIlzZWNwMjU2azGhAlRXBYTyOK9pC1b12RoZDBYN2TbGADcHjecWCNA692xjg3RjcIJ1N4N1ZHCCdTc,enr:-Jy4QPErQaZo6q79ABt2gI_Z_GfgE9bn7nquS8DGv-qcOTxsHtgurfz3vCAuzbyxK7dJsMd4zo0XKN4tCuFWGSykwNCGAZhrFBnAgmlkgnY0gmlwhFtjuPiHb3BzdGFja4IJAIlzZWNwMjU2azGhAqYShcQIhpKP_HXU8slG0VYIwm0cYWOtPdayuAuqwymUg3RjcIJ1OYN1ZHCCdTk
            - --p2p.listen.tcp=9222
            - --p2p.listen.udp=9222
            - --metrics.enabled
            - --metrics.addr=0.0.0.0
            - --metrics.port=7300
            - --syncmode=execution-layer
            - --rollup.config=/rollup.json
            - --log.level=info
        ports:
            - 9545:9545
        volumes:
            - jwt_shared:/jwt
            - op_node_data:/op-node
